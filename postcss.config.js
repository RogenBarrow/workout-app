// Tailwind CSS v4 moved the PostCSS plugin to a separate package.
// Use '@tailwindcss/postcss' instead of 'tailwindcss' here.
export default {
  plugins: {
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
}
  
